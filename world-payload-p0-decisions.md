# World Payload P0/P1 决策定稿

> 范围：仅记录已拍板的 P0 约束，用于后续协议与客户端实现。

## 1. 已确认约束

- `identity` 来自服务端指令建议（suggestion）。
- 当 `identity` 未命中服务端映射时，不创建也不发送 `Payload`。
- 客户端收到的 `Payload` 必须可无障碍反序列化。
- 字段语义统一使用 `absent` 语义（缺省即默认）。
- `worldGen` 总是由服务端映射后的 `Payload` 内给出，客户端不做优先级选择。
- 模板 `region` 拷贝失败时硬失败（对齐原版创建失败处理）。
- `region` 拷贝不做维度额外校验，按运行时直接拷贝区块数据处理。
- `gameRules` 客户端与服务端定义一致；若出现非法值，客户端直接丢弃该项。

## 2. P0 决策表

| Case ID | 触发条件 | 决策 | 客户端行为 |
|---|---|---|---|
| P0-001 | `identity` 未命中服务端映射 | 不发送 payload | 客户端无动作 |
| P0-002 | payload 无法反序列化 | 视为协议错误 | 终止创建并提示错误 |
| P0-003 | 可选字段缺失 | `absent=默认` | 应用本地默认分支 |
| P0-004 | `worldGen` 字段存在 | 直接使用 | 不做优先级竞争/回退分支 |
| P0-005 | 模板 `region` 拷贝失败 | 硬失败 | 中止创建，清理并回退界面 |
| P0-006 | `region` 与维度定义可能不一致 | 不做额外校验 | 仅执行拷贝（风险后置） |
| P0-007 | `gameRules` 某项非法 | 丢弃非法项 | 其余项继续应用 |

## 3. 实现约束（代码落地）

- 服务端：发送前保证 `identity -> payload` 命中，未命中不发包。
- 客户端：反序列化失败直接失败返回，不进入世界创建主流程。
- 客户端：对可选字段统一判 `Optional.empty()/null` 走默认值。
- 客户端：`worldGen` 只读 payload 值，不再执行模板优先级判断。
- 客户端：`region` 拷贝失败与 `createNewWorldDirectory` 同级处理，走硬失败分支。
- 客户端：`gameRules` 应用时逐项校验，非法项跳过并记录日志。

## 4. 已知风险（保留，不阻断 P0）

- 不做 `region`-维度校验可能在运行期暴露异常（例如进入世界后区块/维度语义不一致）。
- `gameRules` 非法项“丢弃”会导致部分规则静默退回默认值，需在日志中明确记录。

## 5. P1 已确认约束（本轮）

- 收到相同 `identity` 请求时：直接进入同名世界，不再走创建流程。
- 创建进行中收到新请求：客户端直接忽视（可配合服务端 ACK 门控避免重复发包）。
- payload 版本兼容：不在业务层做兼容，沿用网络通道版本校验；仅版本一致 payload 会被解析。
- 显式空值：统一使用 `Optional` 表达，依托原版 `Codec` 可选字段写法。
- `gameRules` 非法项处理：丢弃非法项并通过本地 `LOGGER` 记录。
- 拷贝类异常（部分拷贝失败/目录权限/占用等）：统一视为整体失败。
- 世界创建失败后清理：与原版失败清理逻辑保持一致。

## 6. P1 决策表

| Case ID | 触发条件 | 决策 | 客户端行为 |
|---|---|---|---|
| P1-001 | 收到相同 `identity` 请求 | 不重复创建 | 直接进入同名世界 |
| P1-002 | 创建过程中收到新请求 | 忽视新请求 | 保持当前创建流程继续 |
| P1-003 | payload 版本不一致 | 通道侧拦截 | 业务层不解析 |
| P1-004 | 显式空值字段 | `Optional` 语义 | 按空值逻辑落默认/空配置 |
| P1-005 | `gameRules` 项非法 | 丢弃该项 | `LOGGER` 记录后继续 |
| P1-006 | 部分拷贝/拷贝异常 | 视为整体失败 | 中止创建并回退清理 |
| P1-007 | 创建失败 | 执行原版式清理 | 清理目录并回退界面 |

## 7. 进入同名世界判定细则（新增）

- 匹配键：收到 `identity` 后先映射到固定 `folderName`，以 `folderName` 作为唯一匹配键。
- 存在且可打开：若本地存在该 `folderName` 且可正常打开，直接走“进入世界”流程，不再触发创建。
- 不存在：若本地不存在该 `folderName`，进入创建流程。
- 存在但打开失败：按原版失败路径处理（回退界面/提示），不自动重建。
- 创建中收到同 `identity`：视为重复请求，忽略新请求。
- 创建中收到不同 `identity`：按既定规则忽略新请求（可配合服务端 ACK 门控）。
- 配置漂移：若服务端同 `identity` payload 内容变化，但本地同名世界已存在，优先进入已有世界，不重建覆盖。

## 8. P1 扩展决策表（同名进入相关）

| Case ID | 触发条件 | 决策 | 客户端行为 |
|---|---|---|---|
| P1-008 | `identity -> folderName` 匹配且世界存在可打开 | 直接进入 | 调用世界打开流程，不创建 |
| P1-009 | `identity -> folderName` 对应世界不存在 | 创建世界 | 进入创建主流程 |
| P1-010 | 世界存在但打开失败 | 不自动重建 | 按原版失败路径回退 |
| P1-011 | 创建中再次收到同 `identity` | 忽略重复 | 维持当前任务 |
| P1-012 | 已有世界与新 payload 配置不一致 | 进入已有世界 | 不覆盖现有存档 |

## 9. 下一步建议（待你拍板）

- ACK 状态机是否文档化为强约束：`SENT -> RECEIVED -> CREATING -> ENTERED/FAILED`。
- `Optional.empty()` 在 `contentConfig/worldGen` 的业务语义是否拆分为“默认值/显式禁用”两类。
- 是否需要为“进入已有世界”单独增加埋点字段（`identity`、`folderName`、`openResult`、`reason`）。
